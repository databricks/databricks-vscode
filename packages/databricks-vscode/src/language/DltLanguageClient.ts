// import {LanguageClient, Middleware} from "vscode-languageclient/node";

// class DltLanguageClient extends LanguageClient {
//     constructor(name: string, id: string, co: any, so: any) {
//         super(name, id, co, so);
//     }

//     get middleware(): Middleware {
//         r;
//     }
// }
